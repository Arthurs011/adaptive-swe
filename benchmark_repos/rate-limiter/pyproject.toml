[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "rate-limiter"
version = "0.1.0"
packages = ["ratelimit"]

[tool.pytest.ini_options]
testpaths = ["tests"]