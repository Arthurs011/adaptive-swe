[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "text-stats"
version = "0.1.0"
packages = ["textstats"]

[tool.pytest.ini_options]
testpaths = ["tests"]