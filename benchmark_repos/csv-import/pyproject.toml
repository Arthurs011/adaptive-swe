[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "csv-import"
version = "0.1.0"
packages = ["csv_import"]

[tool.pytest.ini_options]
testpaths = ["tests"]