[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "url-toolkit"
version = "0.1.0"
packages = ["urltool"]

[tool.pytest.ini_options]
testpaths = ["tests"]