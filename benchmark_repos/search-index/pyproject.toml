[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "search-index"
version = "0.1.0"
packages = ["searchidx"]

[tool.pytest.ini_options]
testpaths = ["tests"]