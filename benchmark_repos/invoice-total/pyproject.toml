[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "invoice-total"
version = "0.1.0"
packages = ["invoicing"]

[tool.pytest.ini_options]
testpaths = ["tests"]